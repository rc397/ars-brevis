# gold

Private reference solutions. Everything in this directory except this file is gitignored, so nothing here is ever committed or published. The public leaderboard reports byte counts only.

Solutions follow the same convention as `examples/solutions`, one `<task_id>.py` per task defining `p`.
